# Examples

This directory is reserved for sample input documents, templates, and generated outputs.

To keep the repository lightweight in Phase 1, the automated tests generate temporary `.docx` fixtures at runtime instead of committing binary files.

If you want manual sample files, add them here or generate them with a small script based on `python-docx`.
